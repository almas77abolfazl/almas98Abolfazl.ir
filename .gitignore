# Dependencies
node_modules
npm-debug.log
yarn-error.log
pnpm-debug.log

# Build outputs
dist
build
.next
out

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# NestJS specific
*.tsbuildinfo

# Prisma
prisma/migrations

# IDE
.idea
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Testing
coverage
.jest-cache

# Uploaded media at runtime (served by backend at /api/uploads).
# NOTE: `backend/src/uploads/` is SOURCE CODE (the UploadsModule) and MUST be
# committed. Only ignore the runtime upload directory, not every `uploads/` dir.
backend/uploads/

# Logs
logs
*.log
frontend-admin/.angular
frontend-site/.angular

# Kilo config
.kilo
kilo.json
